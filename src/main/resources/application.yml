spring:
  h2:
    console:
      enabled: true
      path: /h2-console

  datasource:
    url: jdbc:h2:mem:toy-hodol;NON_KEYWORDS=USER
    driver-class-name: org.h2.Driver
    username: sa
    password:

#  sql:
#    init:
#      mode: embedded # - 임베디드 데이터베이스(H2, HSQLDB, Derby 등)를 사용할 때만 SQL 초기화 스크립트를 실행

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    defer-datasource-initialization: true # - JPA 엔티티 생성 후 데이터 초기화

jwt:
  secret-key: +uXZZBWa5RGr+ATqTRPgYFladpxz6ndXPeOoxL+rPTI=

